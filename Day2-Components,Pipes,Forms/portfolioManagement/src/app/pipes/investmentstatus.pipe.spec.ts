import { InvestmentstatusPipe } from './investmentstatus.pipe';

describe('InvestmentstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
