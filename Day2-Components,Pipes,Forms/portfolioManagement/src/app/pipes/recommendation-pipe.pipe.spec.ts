import { RecommendationPipePipe } from './recommendation-pipe.pipe';

describe('RecommendationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
